{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block body %}
<style>
  .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
  .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }

  code { 
    background: hsl(220, 80%, 90%); 
  }
</style>
  {# TODO search code move to a component #}
  <div class="search-form">
    <h2>Buscar...</h2>
    <div class="input-group mb-3">
      <input type="text" class="form-control" id="term-search" placeholder="Introduce tu búsqueda" aria-label="Introduce tu búsqueda" aria-describedby="basic-addon2">
      <div class="input-group-append">
        <button class="btn btn-outline-secondary" type="button" id="submit-search">🔍</button>
      </div>
    </div>
  </div>

  {# TODO component list should be a sidebar #}
  <div class="pt-0">
    {% for key, component in list %}
      <div class="single-component">
        <a href="#" id="comp-{{key}}" data-name="{{component}}">{{component}}</a>
      </div>
    {% endfor %}
  </div>
</div>

{# TODO use the space between the search bar and sidebar to print the component asynchronous #}
<div id="comp_async"></div>

{% endblock %}


{% block javascripts %}
  {{ parent() }}
  {{ encore_entry_script_tags('async') }}
{% endblock %}
